name: fetchdata

on:
  workflow_dispatch:
#  schedule:
#    - cron: '5/15 * * * *'
    
jobs:
  fetchdata:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - uses: actions/checkout@v3

      - name: fetchdata
        run: .github/workflows/fetch.sh
